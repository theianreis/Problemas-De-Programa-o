#include <stdio.h>

int main(){

    int contador=1;

    while(contador<=5){
        printf("Contador: %d\n",contador);
        contador++; //incrementa o valor do contador a cada repetição.
    }

    return 0;
}